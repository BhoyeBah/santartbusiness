{% extends 'base.html.twig' %}

{% block title %}Categorieblog{% endblock %}

{% block body %}
    <h1>Categorieblog</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categorieblog.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ categorieblog.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ categorieblog.description }}</td>
            </tr>
        </tbody>
        
    </table>


    <a href="{{ path('app_categorieblog_index') }}">back to list</a>

    <a href="{{ path('app_categorieblog_edit', {'id': categorieblog.id}) }}">edit</a>

    {{ include('backend/categorieblog/_delete_form.html.twig') }}
{% endblock %}
